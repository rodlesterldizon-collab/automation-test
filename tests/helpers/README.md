# Test Helpers

This folder contains reusable helper utilities that can be used by page objects or spec files.

Rules:
- Place shared helper functions here, not test cases.
- Do not put test logic or assertions in helper modules.
- Keep helper functions generic and reusable.
